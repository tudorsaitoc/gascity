[order]
description = "Daemon-owned Dolt history compaction with SQL preflight, verification, and DOLT_GC"
exec = "$PACK_DIR/assets/scripts/compactor.sh"
trigger = "cooldown"
interval = "24h"
timeout = "24h"
